# Department definitions and RBAC tool permissions
#
# Each department defines:
#   allowed_tools: tools this department can use (["*"] = all)
#   budget: optional override for agent loop limits (iterations + tool_calls only)
#
# Time budget (max_wall_time_ms) is controlled globally in config/settings.yaml
# because it depends on hardware/model speed, not on department complexity.
#
# Main agent tools (inspection + routing):
#   read_file, list_files, search_files, excel_inspect
# Host-side tools (registered separately):
#   web_fetch, web_search, read_image, memory_store, memory_recall,
#   dispatch_subagent, send_file
# Subagent-only tools (NOT available to main agent):
#   write_file, edit_file, exec_script, normalize_excel, diff_text,
#   pdf_reader, convert_format, table_query, chart_generate, excel_workbook

departments:
  default:
    description: "Default department for unclassified users"
    allowed_tools:
      - read_file
      - list_files
      - search_files
      - memory_store
      - memory_recall
      - read_image
      - dispatch_subagent
      - excel_inspect
      - web_fetch
      - web_search
      - send_file
    allowed_subagents:
      - research-agent
    budget:
      max_iterations: 10
      max_tool_calls: 20

  engineering:
    description: "Software engineering and IT teams"
    allowed_tools:
      - "*"
    allowed_subagents:
      - filesystem-agent
      - execution-agent
      - document-agent
      - data-agent
      - research-agent
    budget:
      max_iterations: 20
      max_tool_calls: 50

  it:
    description: "IT support and infrastructure"
    allowed_tools:
      - read_file
      - list_files
      - search_files
      - memory_store
      - memory_recall
      - read_image
      - dispatch_subagent
      - excel_inspect
      - web_fetch
      - web_search
      - send_file
    allowed_subagents:
      - filesystem-agent
      - document-agent
      - research-agent
    budget:
      max_iterations: 15
      max_tool_calls: 30

  marketing:
    description: "Marketing and communications"
    allowed_tools:
      - read_file
      - list_files
      - search_files
      - memory_store
      - memory_recall
      - web_fetch
      - web_search
      - send_file
      - read_image
      - dispatch_subagent
      - excel_inspect
    allowed_subagents:
      - document-agent
      - research-agent
    budget:
      max_iterations: 10
      max_tool_calls: 20

  finance:
    description: "Finance and accounting"
    allowed_tools:
      - read_file
      - list_files
      - search_files
      - memory_store
      - memory_recall
      - send_file
      - read_image
      - dispatch_subagent
      - excel_inspect
      - web_fetch
      - web_search
    allowed_subagents:
      - data-agent
      - document-agent
      - research-agent
    budget:
      max_iterations: 10
      max_tool_calls: 20

  hr:
    description: "Human resources"
    allowed_tools:
      - read_file
      - list_files
      - search_files
      - memory_store
      - memory_recall
      - send_file
      - read_image
      - dispatch_subagent
      - excel_inspect
      - web_fetch
      - web_search
    allowed_subagents:
      - document-agent
      - research-agent
    budget:
      max_iterations: 10
      max_tool_calls: 20

  analytics:
    description: "Data analytics and BI"
    allowed_tools:
      - read_file
      - list_files
      - search_files
      - memory_store
      - memory_recall
      - web_fetch
      - web_search
      - send_file
      - read_image
      - dispatch_subagent
      - excel_inspect
    allowed_subagents:
      - data-agent
      - document-agent
      - research-agent
    budget:
      max_iterations: 15
      max_tool_calls: 30

  product:
    description: "Product management"
    allowed_tools:
      - read_file
      - list_files
      - search_files
      - memory_store
      - memory_recall
      - web_fetch
      - web_search
      - read_image
      - dispatch_subagent
      - excel_inspect
      - send_file
    allowed_subagents:
      - document-agent
      - research-agent
    budget:
      max_iterations: 10
      max_tool_calls: 20

  admin:
    description: "System administrators — full access"
    allowed_tools:
      - "*"
    allowed_subagents:
      - filesystem-agent
      - execution-agent
      - document-agent
      - data-agent
      - research-agent
    budget:
      max_iterations: 25
      max_tool_calls: 60

  development:
    description: "Software development team"
    allowed_tools:
      - "*"
    allowed_subagents:
      - filesystem-agent
      - execution-agent
      - document-agent
      - data-agent
      - research-agent
    budget:
      max_iterations: 20
      max_tool_calls: 50
